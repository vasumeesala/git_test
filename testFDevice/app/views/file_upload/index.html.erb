    <div class="row">
	   	<h4> Select Campaign image</h4><br>
        <p>
        <%= direct_upload_form_for @uploader do |f| %>
             <div class="field">
                <%= f.file_field :campimg %>
                <%= f.submit 'Submit', class: 'button small'%>
                <%= link_to 'Back', campaign_path(@campaign), class: 'button small' %>
             </div>
        <%end%>

        </p>
    </div>